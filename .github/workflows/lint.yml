name: lint

on:
  pull_request:
    branches: [master, main]

jobs:
  lint-md:
    runs-on: ubuntu-18.04

    strategy:
      matrix:
        node-version: [14.x]

    steps:
      - run: npm install
      - run: npm run lint:md
